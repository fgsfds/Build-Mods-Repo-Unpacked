// Separate file because we call an appendevent here, and this needs to be the very last thing to happen in that event (as far as drawing UI elements is concerned)
appendevent EVENT_DISPLAYREST
	state showuicursor
endevent
